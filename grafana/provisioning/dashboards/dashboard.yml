apiVersion: 1

providers:
  - name: "Riemann Dashboards"
    orgId: 1
    folder: "Riemann"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
